import { createStore } from 'vuex';
import homeModule from './modules/home';

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    homeModule
  }
})
